[package]
name = "gluon-derive"
version.workspace = true
edition.workspace = true

[features]
tracing = []

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["derive"] }
