/// Test interface
interface Test {
	/// Quit the test
	// oneway transaction
	quit()

	/// Ping the test
	// transaction with empty reply, ensures it got it
	ping() -> ()

	/// Echo the input string
	echo(input: String) -> (output: String)

	/// Echo the ref back
	echo_ref(input: Ref) -> (output: Ref)
}
